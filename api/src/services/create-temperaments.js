const { createTemperaments } = require('../integrations/db/create-temperaments');

const createTemperaments = async (temperaments) => {
    return await createTemperaments(temperaments);
}

module.exports = { createTemperaments };